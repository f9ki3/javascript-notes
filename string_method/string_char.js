const student_name = "Fyke"

//select specific character in the string using the chatAt() method
console.log(student_name.charAt(1))

//it is returning a returns a UTF-16 code (an integer between 0 and 65535).
console.log(student_name.charCodeAt(1))

//same as charAt()
console.log(student_name.at(1))